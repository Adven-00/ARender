project(ARender)

cmake_minimum_required(VERSION 3.1)

set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_STANDARD 17)

file(GLOB_RECURSE CORE_SRC ${CMAKE_SOURCE_DIR}/src/core/*.cpp)
file(GLOB_RECURSE INTERFACE_SRC ${CMAKE_SOURCE_DIR}/src/interface/*.cpp)
file(GLOB_RECURSE PIPELINE_SRC ${CMAKE_SOURCE_DIR}/src/pipeline/*.cpp)

set(TEST_DIR ${CMAKE_SOURCE_DIR}/src/test)
set(CORE_DIR ${CMAKE_SOURCE_DIR}/src/core)
set(INTERFACE_DIR ${CMAKE_SOURCE_DIR}/src/interface)

add_executable(${PROJECT_NAME}
    ${CORE_SRC}
    #${INTERFACE_SRC}
    #${PIPELINE_SRC}
    #${TEST_DIR}/test_output.cpp
    ${TEST_DIR}/test_core.cpp
)

target_include_directories(${PROJECT_NAME} PRIVATE ${CMAKE_SOURCE_DIR}/lib)
